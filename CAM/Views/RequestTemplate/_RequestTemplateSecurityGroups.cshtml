@model RequestTemplateViewModel

<legend>Security Groups</legend>
    
<div class="control-group">
    <fieldset>
        <div class="control-group">
            <label class="control-label">Security Group(s):</label>    
            <div class="controls">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Available</th>
                            <th>Selected</th>
                            <th>Name</th>
                            <th>Description</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach(var sg in Model.GetSecurityGroups())
                        {
                            <tr>
                                <td><input type="checkbox" value="@sg.Value" name="securityAvailable" class="available" @(sg.Available ? "checked='checked'" : string.Empty) /></td>
                                <td><input type="checkbox" value="@sg.Value" name="securitySelected" class="selected" @(sg.Selected ? "checked='checked'" : string.Empty) /></td>
                                <td>@sg.Text</td>
                                <td>@sg.Description</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </fieldset>
</div>